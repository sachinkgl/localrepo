#include<stdio.h>

void main()
{
    printf("If you run git init inside a folder → it becomes a new (nested) repo\n");
    printf("If you dont and just use git add . → it is treated as a normal directory inside the existing repo\n");
    printf("This is a new feature\n");
}